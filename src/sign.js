/**
 * @function sign(num)
 * @description Returns the sign of `num`.
 * @example
 * sign(-45);
 * // -> -1
 * sign(62);
 * // -> 1
 */

export default function sign (num) {
    if(typeof num === "number") {
        return (num < 0) ? -1 : 1;
    }
    return 1:
}

